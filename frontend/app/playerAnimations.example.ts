/**
 * Example usage of player animation functions
 *
 * Import these functions in your components:
 *
 * import { startMovingAnimation, removeAnimation, startTalkingAnimation } from './playerAnimations';
 *
 * Examples:
 *
 * // Start moving animation facing right
 * startMovingAnimation('right');
 *
 * // Start moving animation facing left
 * startMovingAnimation('left');
 *
 * // Remove all animations (return to idle)
 * removeAnimation();
 *
 * // Start talking animation with a message
 * startTalkingAnimation('Hello! How can I help you?');
 *
 * // The talking animation will automatically end after the message duration
 * // Or you can manually stop it by calling removeAnimation()
 */

export {};
